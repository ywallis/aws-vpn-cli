# Copy to vpn.env and fill in your AWS Client VPN endpoint.
# Host = the "remote" line from your .ovpn profile (without the random prefix).
VPN_HOST="cvpn-endpoint-xxxxxxxxxxxxxxxxx.prod.clientvpn.<region>.amazonaws.com"
PORT=443
PROTO=udp
